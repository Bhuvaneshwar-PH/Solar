name: Testing outputs

on:
  workflow_dispatch:

jobs:
  reuse-work:
    uses: ./.github/workflows/reuse.yml
    with:
      Number: ${{ github.run_number }}
      environment: dev
    secrets: inherit

  two:
    environment: dev
    runs-on: ubuntu-latest
    needs: reuse-work
    steps:
      - name: get env from above step
        run: |
          echo "From job one ${{ needs.reuse-work.outputs.NUMBER }}"
          echo "Name is ${{ vars.NAME }}"
